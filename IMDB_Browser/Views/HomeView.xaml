<Page x:Class="IMDB_Browser.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IMDB_Browser.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomeView">

    <StackPanel>
        <!-- Title Type List -->
        <TextBlock Text="Unique Title Types" FontWeight="Bold" Margin="10" />
        <ListBox ItemsSource="{Binding UniqueTitleTypes}" Margin="10" Height="400"/>

        <!--<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10" MaxHeight="350">
            <ItemsControl ItemsSource="{Binding ImageUrls}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Width="200" Height="300" Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>-->
    </StackPanel>
</Page>
